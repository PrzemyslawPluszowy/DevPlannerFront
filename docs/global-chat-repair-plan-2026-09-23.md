# Globalny czat — przegląd i plan naprawy (23.09.2026)

## Zakres i stan dowodów

Repozytoria: Front `/Users/przemyslawnowak/Desktop/dev/DevNote/Front`, Backend `/Users/przemyslawnowak/Desktop/dev/DevNote/Backend`. Punktem odniesienia jest zgłoszenie użytkownika i zrzut z 23.09.2026. To jest przegląd statyczny aktualnego drzewa, bez uruchamiania dodatkowej instancji aplikacji. „Potwierdzone” oznacza zachowanie wynikające wprost z kodu; „do odtworzenia” oznacza, że objaw zgłoszono, ale przyczyny nie można uczciwie rozstrzygnąć bez scenariusza runtime i odpowiedzi API. Nie uznawać istniejących testów jednostkowych za dowód działania interakcji w aplikacji.

## Usterki i braki — kolejność naprawy

| ID | Priorytet | Stan | Usterka i dowód | Oczekiwana naprawa |
| --- | --- | --- | --- | --- |
| C01 | P0 | Naprawione w kodzie; runtime odbiór otwarty | `ChatMessageActionMenu._handle` wywoływał `onPinnedChanged(!isPinned)` po `togglePin`, choć metoda zwracała `Future<void>` i Cubit mógł zapisać błąd bez wyniku dla wywołującego. | `togglePin` zwraca teraz jawny outcome `succeeded/failed/ignored`; menu odświeża przypięcia tylko po `succeeded`. Wspólny `_run` zawsze czyści pending w `finally`, także po wyjątku. Testy Cubita 8/8 PASS; analyze dotkniętych plików i `git diff --check` PASS. Widgetów nie uruchamiano. |
| C02 | P0 | Naprawione w kodzie; runtime odbiór otwarty | Odczyt zależy od widoczności najnowszej wiadomości. Test reprodukował błąd: Cubit oznaczał własną wiadomość jako przeczytaną mimo kontraktu odczytu. Dodatkowo lokalny deduplikator mógł cofnąć identyfikator kursora, gdy widoczność starszej wiadomości przyszła po nowszej. | Cubit pomija własne wiadomości i porównuje kursor zgodnie z porządkiem backendu `(CreatedAtUtc, Id)`, a inbox i globalny badge odświeża po potwierdzonym API. Testy widoczności 6/6 PASS; ręczny odbiór w dwóch sesjach pozostaje otwarty. |
| C03 | P0 | Naprawione w kodzie; runtime odbiór otwarty | `_handlePaste` przechwytywał wklejenie, ale po rozpoznaniu progu zatrzymywał treść na karcie i czekał na ręczny wybór wysłania jako pliku. | Po przekroczeniu progu polityki Front automatycznie przygotowuje i uploaduje dokładny oryginał jako TXT do szkicu. Brak `ChatSnippetRepository` lub błąd przygotowania przechodzi na pełny upload Storage; tekst zostaje w karcie do retry/zachowania po błędzie. Decyzja progów 9/9 PASS, analyze PASS; runtime wklejenia desktop/mobile pozostaje otwarty. |
| C04 | P0 | Naprawione w kodzie; runtime odbiór otwarty | `ChatComposerCubit.updateRichText` emituje stan dla każdej zmiany Delta, a `BlocBuilder` bez `buildWhen` przebudowywał całą powierzchnię composera przy każdym znaku. To niepotrzebnie odtwarzało drzewo aktywnego edytora i mogło opóźniać/zakłócać widoczność wpisywanego tekstu. | `ChatComposerState.shouldRebuildComparedTo` ogranicza rebuild do zmiany trybu, przejścia pusty/niepusty, odpowiedzi, załączników lub wzmianek; kontrolery nadal renderują tekst/Delta bezpośrednio. Test polityki 2/2 PASS, analyze PASS. Odbiór IME/desktop/mobile na żywo pozostaje otwarty; testów widgetowych nie uruchamiano. |
| C05 | P0 | Naprawione w kodzie; runtime odbiór otwarty | Ostatni Owner dostawał 400, a UI pokazywało ogólny błąd; menu zmian ról ukrywało Ownera, więc nie było widocznej ścieżki przekazania własności. Po opuszczeniu sheetu wynik `true` nie czyścił aktywnej rozmowy. Backend już wspiera transakcyjne przekazanie roli Owner i democję dotychczasowego Ownera do Moderator. | Owner może wybrać „Przekaż własność” na członku; jeśli jest jedynym Ownerem, akcja opuszczenia jest zablokowana i wyjaśniona. Błąd wyścigu pokazuje to samo objaśnienie. Po potwierdzonym leave panel wraca do inboxa, dzięki czemu realtime lease jest zwalniany, a inbox/unread są odświeżane. Testy members/search 15/15 PASS, analyze PASS; runtime wielosesyjny otwarty. |
| C06 | P1 | Potwierdzone | Reakcja z menu otwiera `showBottomSheet` (`chat_message_action_menu.dart:262-300`), forward otwiera centralny `showDialog` (`chat_message_action_dialogs.dart:114-207`). | Zakotwiczone menu reakcji przy dymku i zakotwiczony, wyszukiwalny wybór rozmowy przy akcji forward, z pozycjonowaniem przy krawędziach, klawiaturą i spójnym chat theme. Modal tylko tam, gdzie wymagana decyzja o dużej wadze. |
| C07 | P1 | Potwierdzone | Dymek ma `onSecondaryTapUp` (`chat_message_bubble.dart:106-111`), a cała lista jest w `SelectionArea` (`chat_panel_conversation_parts.dart:775-780`); oba regiony mogą reagować na ten sam prawy klik po zaznaczeniu tekstu. | Jeden arbiter menu: przy zaznaczeniu tekstu kopiuj/zaznacz wszystko w jednym menu, poza zaznaczeniem akcje wiadomości. Wyłączyć drugie menu w tej ścieżce; zachować zaznaczenie i dostępność. |
| C08 | P1 | Potwierdzone | Karty plików przed wysyłką pokazują nazwę i status bez obrazu (`chat_attachment_composer_controls.dart:76-92,140+`). Historia ma miniaturę, ale jej brak kończy się cicho ikoną (`chat_message_attachments.dart:95-118`); adapter pobiera pełne bajty przez ticket (`chat_attachment_access_port_adapter.dart:39-45`). | Lokalna miniatura natychmiast po wyborze zdjęcia, potem autoryzowana miniatura po wysłaniu; jawny stan ładowania i błąd podglądu. Rozróżnić nieudany upload, skan, brak ticketu i dekodowanie obrazu. Ograniczyć pamięć/rozmiar pełnego obrazu. |
| C09 | P1 | Potwierdzone jako brak UI | Composer oferuje tylko platformowy picker (`chat_message_composer.dart:899-924`), nie wybór istniejącego prywatnego pliku Storage. Backend Chat zna `storageFileId` w załączniku (`ChatEndpoints.cs:734-749`), lecz trzeba zweryfikować ACL i kontrakt ponownego użycia pliku. | Dodać „Z prywatnych plików” w menu załącznika, picker drzewa Storage i bezpieczne dołączenie przez serwer. Nie używać lokalnego uploadu jako obejścia, nie ujawniać plików bez uprawnień. Jeśli API nie dopuszcza istniejącego pliku, dodać mały endpoint/use case z walidacją właściciela i ACL rozmowy. |
| C10 | P1 | Potwierdzone | Emoji picker używa `PopupMenuItem(enabled:false)` z interaktywnym dzieckiem (`chat_emoji_picker.dart:33-51`), domyślnych styli `TextButton` i prostych kolorowych kropek odcieni (`:150-175,221-258`). `ChatEmojiSkinTone.apply` dokleja modyfikator na końcu całego klastra (`chat_emoji_catalog.dart:65-86`), co jest niepoprawne dla części emoji z ZWJ i złożonymi sekwencjami. | Własny overlay chat theme, pełna nieprzezroczysta siatka i czytelne próbki odcieni na emoji, poprawne sekwencje Unicode dla wspieranych wpisów; test wizualny na macOS/web w jasnym i ciemnym motywie. Rozwiązać wybór bez wyłączonego `PopupMenuItem`. |
| C11 | P1 | Potwierdzone | Zrzut pokazuje ciasne, zawinięte filtry. Kod używa poziomego scrolla i siedmiu `ChoiceChip` (`chat_inbox_list.dart:217-267`), więc wąski panel ujawnia tylko początek listy, bez wskazania dalszych kategorii; komponent ma styl Material, nie docelowy chat theme. | Przeprojektować nagłówek: czytelna pierwsza linia rozmów i jedno menu filtrów lub zakładki z wyraźnym overflow; grupy, kanały, pliki i zadania zgodnie z zakresem produktu. Sprawdzić 320/360/420 px, skalowanie tekstu i zrzut użytkownika. |
| C12 | P2 | Potwierdzone | `ChatMessageActionMenu.showAt` pobiera stan pin/bookmark z jednego snapshotu przy otwarciu (`:71-85`), a callback przypięcia nie aktualizuje samego `message`; po mutacji menu może pokazywać stary stan do ponownego odczytu. | Jedno źródło prawdy dla stanu akcji, aktualizacja po sukcesie i odświeżenie serwerowe przy ponownym otwarciu; analogicznie bookmark i reakcje. |
| C13 | P2 | Potwierdzone | `ChatAttachmentAccessPortAdapter.thumbnail` pobiera cały plik, choć nazywa to miniaturą (`:39-45`); przy dużych zdjęciach rośnie pamięć i opóźnienie, a błąd jest zamieniany w `null`. | Użyć limitowanej miniatury Storage lub bezpiecznego strumienia z limitem, osobno raportować porażkę i udostępnić retry. Nie pobierać pełnego pliku dla każdej karty listy. |
| C14 | P1 | Potwierdzone | `ChatStatusMenuButton` zapisuje `_current`, ale przycisk zawsze rysuje `widget.icon ?? Symbols.mood` (`presence/chat_status_menu.dart:195-207`); status nie jest widoczny na belce po zapisie ani po otwarciu panelu. | Załadować status przy montowaniu i pokazywać jego emoji jako ikonę; po zapisie natychmiast aktualizować trigger, po usunięciu przywracać ikonę bazową. DND/status wygasły ma czytelny stan i tooltip. |
| C15 | P1 | Zgłoszone, przyczyna do odtworzenia | Użytkownik ponownie zgłosił, że wyszukiwarka nic nie zwraca. `ChatSearchCubit` wysyła zapytanie dla min. 2 znaków i `ChatSearchRepositoryImpl` mapuje API; Front i Backend używają `q`, a backend odrzuca frazę krótszą niż 2 znaki. W kodzie statycznym nie ma oczywistego błędu serializacji. | Odtworzyć dla świeżej i starej wiadomości, frazy ASCII/PL, frazy w treści kodu, DM/grupy. Zalogować bez danych wrażliwych request `q`, status i liczbę `items`; sprawdzić `SearchText` w rekordach oraz indeks/search backendu i kontrakt JSON. UI ma rozróżnić ładowanie, brak wyników i błąd. |
| C16 | P0 | Poprawione w kodzie; czeka na weryfikację runtime | Przyczyna `ProviderNotFoundException` była potwierdzona: `ChatThreadSidePanel` czyta `ChatConversationRepository` i `ChatDraftRepository`, ale rootowy sheet dostarczał tylko `ChatThreadRepository`. `ChatThreadSheet.showThread` przyjmuje teraz wszystkie trzy zależności, tworzy dla nich `RepositoryProvider`, a wywołanie w `chat_drawer.dart` przekazuje conversation repository (z fallbackiem do `ChatRepository`, którego implementacja spełnia ten port) i draft repository. Pozostaje sprawdzić ręcznie otwarcie wątku w działającej aplikacji; testów nie uruchamiano zgodnie z ustalonym zakresem weryfikacji. | Audytować każdy `context.read` w root modalach. Przy braku wymaganej kompozycji pokazać jawny stan błędu zamiast bezgłośnie ignorować otwarcie. Sprawdzić AuthSessionPort i pozostałe opcjonalne zależności. |
| C17 | P1 | Cytat dodany; skok do oryginału nadal do zrobienia | `replyToMessageId` przechodził przez draft, optimistic send i API (`ChatComposerDraft`, delivery queue, `ChatRepositoryImpl`), lecz `ChatMessageBubble` go nie renderował. Dodano cytat w dymku, z fragmentem oryginału, nazwą autora tam, gdzie jest dostępna, oraz komunikatem dla usuniętego/niedoładowanego posta. Powiązany post jest teraz widoczny po wysłaniu. | Dodać klik cytatu, który przewija i podświetla oryginał. Jeśli oryginał nie jest w pobranej stronie, dociągnąć go przez bezpieczny endpoint okna wiadomości. Obsłużyć długi tekst i reply do lokalnej/niepotwierdzonej wiadomości. |
| C18 | P0 | Potwierdzone | `ChatRichTextBody` rozpoznaje bloki kodu, ale `_CodeBlock` renderuje zwykły `Text` monospace, bez tokenizacji; powierzchnia pochodzi z motywu, nie jest czarna. `ChatCodeBlockCodec.build` wstawiał cały multiline kod jako jeden op i tylko jedną sformatowaną nową linię, więc Quill nadawał format bloku wyłącznie ostatniej linii. | Dodać ciemny kafelek kodu i kolorowanie składni dla języków z dialogu; zachować monospace, przewijanie poziome, kopiowanie, etykietę języka i bezpieczny plain fallback. Każda linia Delta musi dostać format code-block, a zapis i odczyt API muszą zachować język i granice bloku. |
| C19 | P0 | Potwierdzone w kodzie; raportowane runtime | `ChatMentionCodec.labelFor` zwraca surowy `userId`, jeśli katalog podpowiedzi nie dostarczy `displayName` ani `login`. `_acceptMention` wstawia tę etykietę do pola, a `ChatConversationCubit.sendDraft` zamienia ją na `@UUID` przed utworzeniem optymistycznej wiadomości; `ChatRichTextBody` nie renderuje tokenu z powrotem jako etykiety. Zgłoszony tekst `@a8878671-6c43-40bd-bbcd-c95654c3f90e` pasuje do tego przepływu. | Nigdy nie używać UUID jako etykiety UI. Zagwarantować czytelną etykietę w podpowiedzi i renderować transportowe `@UUID` jako nazwę w historii po wysłaniu, również po odświeżeniu i w urządzeniu drugiego członka. Zachować token UUID wyłącznie w transporcie i identyfikację wzmianki po stronie backendu. |
| C20 | P1 | Potwierdzony brak akcji UI | `ChatConversationManagementRepository.updateDetails` i backendowy `PATCH /conversations/{id}` istnieją, a serwer pozwala Owner/Moderatorowi zmienić nazwę. W menu nagłówka i członków brak formularza/akcji zmiany nazwy, więc użytkownik nie może jej zmienić. | Dodać do menu rozmowy akcję „Zmień nazwę” dla Owner/Moderator, edytor z walidacją i zapis przez istniejący `updateDetails`; aktualizować nagłówek i inbox po sukcesie, pokazać błąd serwera przy porażce. Rozmowy Direct nie powinny dostać akcji zmiany nazwy grupy. |
| C21 | P1 | Zgłoszone; pierwotna interpretacja sprostowana przez użytkownika | Zrzut zawiera ucięty przy lewej krawędzi awatar innego uczestnika, ale użytkownik doprecyzował, że zgłoszony problem dotyczy wiadomości własnych: ostatnie trzy napisał bieżący użytkownik, a ostatnia jest biała i wyrównana jak cudza. Nie zakładać, że problemem jest awatar. | Sprawdzić authorUserId w wiadomości potwierdzonej oraz optymistycznej. Własne wiadomości muszą być wyrównane i pokolorowane po stronie nadawcy, także podczas wysyłki, błędu i po ponownym uruchomieniu. Sprawdzić różnicę plain/rich composer. |
| C22 | P0 | Potwierdzone w kodzie | `ChatMessageDeliveryQueue.enqueue` oraz `restorePending` tworzą lokalną wiadomość z `authorUserId: ''`. Dymek wyznacza własność wyłącznie przez porównanie `authorUserId == currentUserId`, zatem wiadomość oczekująca lub wznowiona jest biała/lewa do momentu potwierdzenia serwera. | Wstawiać kanoniczne `userId` kolejki do optymistycznej wiadomości. Zachować tożsamość podczas retry i wznowienia; po potwierdzeniu zastąpić server-author ID. |
| C23 | P1 | Zgłoszone; do potwierdzenia kontraktu | Użytkownik zgłasza brak podwójnego znacznika przy odczytanej wiadomości. Metadata już wybiera `done_all`, gdy `readByCount > 0` (`messages/chat_message_metadata.dart`), ale trzeba sprawdzić, czy serwer zwraca licznik czytających i czy UI aktualizuje go po read realtime bez ponownego fetchu. | Odtworzyć na DM i grupie na dwóch kontach. Zweryfikować wartości `readByCount`, event read/delivery i mapper; autor widzi jeden znacznik dostarczenia oraz podwójny po potwierdzonym odczycie, z aktualizacją realtime i po ponownym wejściu. Nie zgadywać lokalnie statusu odczytu. |
| C24 | P1 | Potwierdzona niespójność architektury | `chat_message_composer.dart` ma 1149 linii i skupia lifecycle, edycję Quill/plain, wzmianki, wklejanie/TXT, załączniki, emoji, skróty klawiaturowe i budowę całego UI. Standard repo wymaga limitu 400 linii dla ręcznie pisanego widgetu/klasy; ogólna zasada mówi, że widoki zwykle powinny mieć poniżej 300. `AGENTS.md` wcześniej opisywał podział dużych plików, ale nie określał jawnego limitu 400 ani języka dokumentacji. | Osobny refaktor po ustabilizowaniu napraw zachowania composera: rozbić odpowiedzialności na wyspecjalizowane komponenty/koordynatory o jawnym lifecycle, każdy plik/klasa poniżej 400 linii (cel zwykle <300), bez `part`/mixin jako obejścia. Zachować publiczny kontrakt composera i bieżące zachowania; zaktualizować polski handoff. Nie mieszać tego z bieżącymi poprawkami wysyłki/Quill. |
| C25 | P0 | Przyczyna Front potwierdzona; poprawka i test jednostkowy PASS; staging do sprawdzenia | Pięć kolejnych `POST /api/v1/chat/conversations/{id}/messages` zakończyło się `400 validation.failed` z treścią „Wartość atrybutu Delta wiadomości Chat musi być wartością prostą JSON.” Trace ID: `0HNOPHJ6073M9:00000001` oraz `0HNOPHJ6073MA:00000001`–`0HNOPHJ6073MD:00000001` (23.09.2026). `ChatLineFormatCommands.toggledValue` oraz `applyLineFormatCommand` serializowały atrybut `list` jako tablicę `['bullet']`/`['ordered']`, mimo że backend `ChatMessage.ValidateDelta` odrzuca tablice, a renderer historii oczekuje wartości tekstowej. Pozostałe operacje formatowania composera tworzą wartość logiczną albo tekstową; `ChatCodeBlockCodec` tworzy skalarne atrybuty bloku. | Wysyłać wartości `list` jako skalarne `'bullet'`/`'ordered'`, zgodne z Quill, backendową walidacją i rendererem; zachować formatowanie po odczycie i edycji. Potwierdzić stagingowym wysłaniem wiadomości z listą oraz odczytem delty. Nie logować tekstu ani pełnej treści delty użytkownika. |
| C26 | P1 | Zgłoszone; część przyczyn potwierdzona statycznie, formaty kolorów/czcionki wymagają doprecyzowania kontraktu | Użytkownik zgłasza w zaawansowanym edytorze niewidoczne pogrubienie, numerację i kropki list oraz niejasne kolory/czcionkę. `ChatComposerRichTextField` nie przekazywał `customStyles`, więc Quill brał style domyślne z `ThemeData`, a nie z `ChatTheme`. Renderer historii rozumie już listy, ale wcześniejsza tablica atrybutu `list` uniemożliwiała Quill/backendowi poprawny zapis (C25). Bold historii był `w600`. Backend dopuszcza atrybuty `color`, `background`, `font`, `size`, ale toolbar nie daje tych opcji, a renderer ich nie interpretuje. | Ujednolicić Quill z ChatTheme i czytelną typografią, zweryfikować w runtime bold/listy i ich widoczność zarówno podczas pisania, jak i po wysłaniu. Zdecydować i opisać obsługiwane opcje koloru/rodziny/rozmiaru fontu; jeśli są dostępne w edytorze, zdefiniować bezpieczną paletę i dozwolone wartości po obu stronach oraz zachować je w Delta/rendererze. Nie oferować formatów, które znikają po wysłaniu. |
| C27 | P1 | Poprawione w kodzie; runtime do odbioru | Użytkownik potwierdza, że bold nadal był niewidoczny, listy już widać, a cytat nie. W edytorze bold miał tylko `w700`, a renderer historii również `w700`; cytat w Quill miał pustą dekorację, a historia używała słabo kontrastowego separatora bez tła. | Bold w aktywnym edytorze i historii jest teraz `w900`; cytat ma kontrastowy akcent `ChatTheme.focusRing`, tło i padding w obu miejscach. Potwierdzić oba motywy i edycję/odczyt na żywo po wdrożeniu do aplikacji. Widget/golden nadal odroczone do akceptacji UI. |
| C28 | P0 | Naprawione w Backend i wdrożone na staging; request-level POST nie został sztucznie odtworzony, aby nie pisać do cudzej rozmowy. | Trace `0HNOPHJ607482:00000001` i `0HNOPHJ607483:00000001` pokazały, że Quill wysyła `attributes.code`, a allowlista Backend nie dopuszczała tego atrybutu. `ChatMentionParser` i Front renderer już go obsługiwały. | Dodano `code` do allowlisty oraz test dokładnej delty z wszystkimi formatami obecnego composera. Testy Delta 7/7 PASS; pełna suite Backend przed dodaniem samego testu zbiorczego 1307 PASS / 4 SKIP / 0 FAIL. Commit `446116a45b387db64f8affcc56a679c0013622b2` wdrożono ręcznym skryptem; API healthy i `/health/ready` zwraca `Healthy`. Nie dotyczy schematu. |

## Pakiety implementacyjne dla kolejnego agenta

### A. Stabilność i stan serwera

1. Odtworzyć C01/C02/C05 na jednym koncie oraz na dwóch kontach, zapisać request, status, `code`, traceId bez sekretów. Sprawdzić DM, grupę, kanał, Ownera, Moderatora, członka i osobę bez ACL.
2. Naprawić przepływ read w `ChatPanelMessageList`, `ChatConversationCubit`, `ChatInboxCubit`, `ChatUnreadCubit`; potwierdzenie serwera musi aktualizować licznik oraz listę bez ponownego otwierania. Backend zmieniać tylko, gdy scenariusz pokaże wadę `MarkReadAsync`/inbox query.
3. Zmienić kontrakt Cubita akcji z `Future<void>` na jawny wynik dla pin/unpin/bookmark/forward/reaction; callbacki tylko po sukcesie. Zablokować duplikaty w trakcie żądania; zachować realny `code` błędu.
4. Po udanym leave wykonać jeden przepływ wyjścia przez właściciela panelu, odłączyć subskrypcje i wyczyścić historię z pamięci. Obsłużyć odmowę ostatniego Ownera i utratę ACL w locie.
5. Bramka po pakiecie: testy logiki Cubit i kontraktu REST dla read/pin/leave (sukces, błąd, race, ACL). Jeden ręczny przebieg w aplikacji; bez serii testów przy każdym pliku.

### B. Composer i załączniki

1. Ujednolicić paste z klawiatury, kontekstowego menu i Quill. Próg TXT z serwerowej `ChatLinkPolicy`; zachować dokładne bajty tekstu, limit pliku i rozsądny komunikat o przygotowaniu. Scenariusze: krótkie, długie, multiline, Unicode, wklejenie przy zaznaczeniu, tryb plain/rich, nieudany upload i retry.
2. Usunąć wymianę kontrolera podczas aktywnej edycji albo wymusić natychmiastowe przepięcie Quill; sprawdzić wpisywanie w czasie rzeczywistym, formatowanie, undo/redo, `@mention`, kod i draft po powrocie.
3. Dodać lokalny podgląd zdjęcia w karcie composera; po wysyłce sprawdzić ticket, odpowiedź i renderer. Dostęp do obrazów przez ACL Storage; nie utrwalać presigned URL w szkicu.
4. Dodać picker prywatnych plików i dopiero po audycie backendowego `AttachAsync` zdecydować, czy istniejący kontrakt wystarczy. Jawnie porównać źródło pliku, uprawnienia do odczytu, ACL rozmowy, status skanowania, deduplikację i lifecycle po usunięciu.
5. Bramka po pakiecie: testy logiki paste/Quill/upload oraz pojedynczy przebieg desktop + web z jednym małym i dużym obrazem. Testy widgetowe dopiero po zatwierdzeniu UI przez użytkownika; testów golden nie tworzyć.

### C. Powierzchnie interakcji i wygląd

1. Zbudować jeden system zakotwiczonych popoverów czatu dla reakcji, forward, emoji, filtrów i menu wiadomości. Użyć tokenów `ChatTheme`; nie pozwolić Material na domyślne kolory/tint ani przezroczyste ikony. Pozycjonowanie sprawdzić po lewej/prawej krawędzi i przy dolnej krawędzi panelu.
2. Rozwiązać konflikt `SelectionArea`/dymek przez jedno menu kontekstowe. Pierwszy prawy klik na zaznaczeniu nie może otwierać dwóch nakładek; skróty kopiowania i zaznaczania muszą nadal działać.
3. Przeprojektować filtr inboxa na wąskiej szerokości zgodnie ze zrzutem użytkownika. Zachować osobne wejścia dla rozmów bezpośrednich, grup, kanałów oraz planowanych kanałów plików i zadań; nie eksponować nieaktywnych funkcji jako działających.
4. Picker emoji: zamienić próbki odcieni na rzeczywiste emoji, naprawić generowanie wariantów Unicode, kontrast i opacity, dostępność, klawiaturę oraz ostatnio używane. Przetestować quick reactions i status.
5. Bramka po pakiecie: ocena wizualna prawdziwej aplikacji przez użytkownika przed pisaniem testów widgetowych. Nie tworzyć goldenów. Potem jedna zbiorcza seria testów widgetowych dla zaakceptowanego UI.

## Kontrola dodatkowych błędów podczas pakietów

Przejść cały cykl: utworzenie DM/grupy/kanału, dodanie/usunięcie osoby, rola i Owner, wiadomość/reply/thread/mention, edycja/usunięcie, reakcja, pin/bookmark, forward, draft, załącznik lokalny i prywatny, plik duży, wyszukiwanie, unread/read, archiwum, mute, realtime offline/reconnect, utrata ACL. Dla każdej akcji zweryfikować loading, sukces, błąd, powtórzenie, nawigację i stan po restarcie. Odkryte usterki dopisać do tabeli z plikiem, linią i reprodukcją przed implementacją. Nie deklarować „wszystkich błędów” bez przejścia tych ścieżek.

Przy każdej zmianie DTO/API wykonać audyt wszystkich enumów dotkniętego przepływu: klasyfikacja request/response/persistence/UI, dokładne wartości JSON w C#, OpenAPI, odpowiedzi i kliencie Flutter wraz z `.g.dart`, test encode/decode każdej wartości i fallback. To wymaganie obowiązuje również wtedy, gdy sam enum się nie zmienia. Backend wdrażać na staging tylko gdy pakiet rzeczywiście zmieni backend i po przejściu bramek; nie uruchamiać wielu instancji aplikacji. Nie commitować ani pushować bez polecenia.

## Kryterium zakończenia

Każdy C01–C28 ma dowód reprodukcji lub odrzucenia, zmianę z potwierdzonym wynikiem, ręczne sprawdzenie w aplikacji, oraz aktualizację checklisty i handoff w obu repozytoriach. Nie wystarczy zielony build. Najpierw użytkownik akceptuje UI, potem wykonywana jest zbiorcza weryfikacja widgetowa; bez goldenów.

## Postęp napraw

### Audyt kontraktu Quill ↔ Backend (2026-09-23)

Źródła sprawdzone bezpośrednio: Front `chat_composer_rich_toolbar.dart`,
`chat_format_commands.dart`, `chat_format_actions.dart`,
`chat_message_composer_fields.dart`, `chat_rich_text_codec.dart` i
`chat_code_block_codec.dart`; Backend `Domain/Entities/ChatMessage.cs` oraz
`Application/Chat/ChatMentionParser.cs`. Backend przyjmuje tablicę operacji
Quill (`insert` tekst lub obiekt) i limituje nazwy atrybutów allowlistą, lecz
poza prostymi typami JSON nie waliduje ich dozwolonych wartości.

| Format/wartość Delta | Toolbar Quill | Backend | Renderer historii Front | Ocena |
|---|---|---|---|---|
| `bold: true`, `italic: true`, `strike: true`, `code: true` | Tak; `code` to Quill `inlineCode` | Dozwolone (`code` dodane w CHAT-R78) | Wszystkie renderowane; kod dostaje styl monospace/tło | Zgodne na poziomie klucza i wartości |
| `link: URL` | Tak, tylko HTTP/HTTPS lub ścieżka `/` po normalizacji | Dozwolone, backend wymaga absolutnego HTTP/HTTPS bez userinfo | Renderowany i klikalny tylko przy bezpiecznym URL HTTP/HTTPS lub ścieżce `/` | Zgodne dla URL absolutnych HTTP/HTTPS; ścieżka względna jest odrzucana przez Backend — potencjalny rozjazd, nie wysyłać względnych linków do Chat |
| Listy Quill `ul`/`ol` | Tak; `Attribute.ul`/`Attribute.ol` zapisują standardowe klucze `list: bullet` / `list: ordered` | `list` dozwolone; wartość skalarna przechodzi | Renderer rozpoznaje dokładnie `bullet` i `ordered` | Zgodne; nie mapować na literalne `ul`/`ol` w JSON |
| `blockquote: true`, `code-block: true` lub język tekstowy | Tak; cytat/blok kodu; osobny „Wstaw kod” zapisuje język | Dozwolone; wartości są skalarne | Cytat i code block renderowane; code block zachowuje opcjonalny język | Zgodne |
| `underline: true` | Brak przycisku w bieżącym toolbarze | Dozwolone | Renderer obsługuje | Backend/history wspierają, ale użytkownik nie może włączyć tego przez aktywne UI |
| `header`, `indent`, `align`, `direction`, `color`, `background`, `font`, `size`, `script`, `formula`, `image`, `video` jako atrybuty | Brak w bieżącym toolbarze | Dozwolone, URL-e dla `image`/`video` są walidowane | W większości nieuwzględnione w stylowaniu/blokach historii | Backend ma szerszą allowlistę niż produkt; zapis może przejść bez widocznego efektu lub utracić format w rendererze |
| Embed `insert: {image|video|link: ...}` | Aktywny composer nie wstawia takiego embedu; obrazy/pliki idą przez załączniki | Rozpoznane URL-e waliduje; obecna pętla nie odrzuca jawnie nieznanych nazw embedów | Renderer pokazuje embed jako nieobsługiwany znacznik, nie renderuje obrazu/wideo | Niezgodne/ryzykowne: odrzucać nieznane embed-y lub wdrożyć pełną obsługę; załączniki są jedyną aktywną ścieżką obrazów |

Wnioski do kolejnej poprawki: (1) zawęzić backendową allowlistę do świadomie
obsługiwanych formatów albo dodać pełne mapowanie/renderowanie przed
udostępnieniem szerszych formatów; (2) walidować typ i wartość każdego
atrybutu, nie tylko „wartość prostą JSON”; (3) jawnie odrzucać nieznane embed-y;
(4) zdecydować, czy `underline` ma dostać przycisk toolbaru; (5) ujednolicić
kontrakt linków — aktywne UI może przyjąć ścieżkę `/`, walidator wiadomości
obecnie wymaga absolutnego HTTP/HTTPS. Nie zmieniano teraz kodu ani nie
uruchamiano kolejnych testów; to audyt źródeł. Przed uznaniem formatowania za
gotowe dodać test kontraktowy Front → dokładny JSON Delta → Backend walidacja →
renderer historii dla każdej oferowanej opcji.

- C05: backend role update już obsługiwał transakcyjne przekazanie własności. Front pokazuje „Przekaż własność” właścicielowi, blokuje leave jedynego Ownera z czytelnym wyjaśnieniem i obsługuje potwierdzony wynik opuszczenia: wraca do inboxa oraz odświeża liczniki. Testy `g5_search_and_members_test.dart` 15/15 PASS, analyze zakresu PASS, diff check PASS. Ręczny scenariusz Owner → transfer → leave na dwóch sesjach nadal otwarty.
- C03: długie wklejenie po przekroczeniu polityki samo uruchamia dołączenie TXT; oryginał pozostaje w stanie karty, a brak/błąd Snippet API korzysta z pełnego Storage upload. Krótkie wklejenie nadal wstawia tekst normalnie. `chat_long_paste_decision_test.dart` 9/9 PASS, analyze dotkniętego zakresu PASS, `git diff --check` PASS. Test/widget runtime clipboard pozostaje otwarty.
- C04: potwierdzono, że każda zmiana rich text emitowała Cubit state i przebudowywała całe drzewo composera. Dodano `buildWhen` przez politykę stanu: rebuild tylko po zmianie trybu, dostępności treści, odpowiedzi, listy załączników lub wzmianek. Test `chat_composer_state_test.dart` 2/2 PASS, `dart analyze` dotkniętych plików PASS, `git diff --check` PASS. Widget/runtime odbiór pozostaje otwarty.
- C02: test przed poprawką potwierdził mark-read dla własnej wiadomości. Cubit teraz pomija wiadomości bieżącego autora i utrzymuje lokalny kursor monotonicznie według `(CreatedAtUtc, Id)`, zgodnie z Backend. Test `chat_conversation_read_visibility_test.dart` 6/6 PASS, analiza dotkniętych plików PASS, diff check PASS. Odświeżenie inboxa/globalnego badge po sukcesie jest już w kodzie; odbiór dwóch sesji i runtime pozostaje otwarty.
- C16: brakujące providery rozmowy i draftów przekazywane są jawnie do rootowego sheetu wątku. Brak testów i ręcznej weryfikacji runtime.
- C18: dodano ciemną powierzchnię i tokenizację składni; codec generuje format bloku dla każdej linii. Brak testów i walidacji wizualnej w runtime.
- C17: dymek renderuje teraz cytat odpowiedzi; lokalizacje PL/EN wygenerowano. `dart analyze` zmienionych widgetów przechodzi; brak testów i ręcznej walidacji wizualnej.
- C19: potwierdzono ścieżkę transportową, która może pokazać UUID zamiast czytelnej etykiety. Naprawa etykiety i renderowania wzmianki jest do wykonania.
- C20: istnieje kontrakt zmiany nazwy w backendzie i repozytorium Front; brak akcji/formularza UI.
- C21: użytkownik sprostował pierwotną obserwację; ucięty awatar na zrzucie nie jest zgłoszonym błędem i nie należy go diagnozować w ramach C21.
- C21: użytkownik sprostował, że istotny problem na zrzucie to biała, lewa wiadomość napisana przez siebie; plan skorygowano i poprzednia hipoteza o awatarze cofnięta.
- C22: wykryto potwierdzoną przyczynę białych optymistycznych wiadomości: puste `authorUserId` w obu ścieżkach tworzenia kolejki.
- C23: backend emituje `chat.message.read` i `chat.message.delivered`, ale Front wcześniej klasyfikował je jako `unsupported` (pełny resync), a przy oknie starej wiadomości pomijał wszystkie eventy. Dodano typowany event zmiany dostarczenia/odczytu; reducer zleca pobranie autorytatywnego okna tej samej wiadomości, a Cubit scala rekord również w historii okienkowej. Testy mappera/reducera **14/14 PASS**, `dart analyze` dotkniętych eventów, reducera, mappera, Cubita i testów PASS; odbiór dwukontowy pozostaje otwarty.
- C24: potwierdzono, że `chat_message_composer.dart` ma 1149 linii. Uzupełniono `AGENTS.md`: 400 linii to twardy limit ręcznie utrzymywanych plików/klas produkcyjnych, a mniej niż 300 jest celem dla typowych widgetów. Refaktor composera pozostaje osobnym pakietem po ustabilizowaniu bieżących napraw; nie wykonano mechanicznego przenoszenia kodu.
- C25: pięć stagingowych 400-ek miało wspólną przyczynę w formacie listy Quill: Front wysyłał `attributes.list` jako tablicę. Zmieniono wartość modelu i wywołanie Quill na jego skalarne atrybuty `ul`/`ol`; renderer już rozumiał tekstowe `'bullet'`/`'ordered'`. Przejrzane pozostałe operacje formatowania nie tworzą zagnieżdżonych atrybutów. Test `chat_format_commands_test.dart` **12/12 PASS**, `dart analyze` zakresu PASS, `git diff --check` PASS. Backend/API nie wymaga zmiany. Potwierdzenie wysyłki i odczytu na stagingu pozostaje otwarte.
- C26: pole Quill dostało jawne `DefaultStyles` z `ChatTheme` dla akapitu, list, cytatu, kodu inline/bloku, pogrubienia i linku; renderer historii początkowo wzmacniał bold do `w700`; w poprawce C27 podbito go do `w900`. `dart analyze` dotkniętych plików PASS; widgetów/goldenów i runtime nie uruchamiano. Opcje koloru/tła/rodziny/rozmiaru fontu pozostają nierozstrzygniętym zakresem; nie są obecnie eksponowane w toolbarze ani renderowane.

- C27: użytkownik zgłasza ponownie niewidoczne bold i markery `123.`/kropek. C26 nie ma jeszcze odbioru wizualnego; font bazowy to 14 px bez jawnej rodziny, a toolbar nie zawiera wyboru fontu/koloru/rozmiaru. Weryfikacja musi objąć jasny/ciemny motyw, edycję na żywo, stan aktywnego formatowania i treść po wysłaniu. Nie dodawać formatów, które nie przechodzą walidacji backendu i renderera historii.

- C28: błąd stagingowy `attributes.code` został odtworzony na podstawie logu i potwierdzony w kodzie walidatora: Quill inline code jest obecny w toolbarze, parserze wzmianek i rendererze, ale backend nie dopuszczał klucza. Front/Backend zmienione lokalnie; testy Front 25/25 PASS, testy Delta Backend 7/7 PASS; pełna suite Backend (przed dodaniem samego zbiorczego testu) 1307 PASS / 4 SKIP / 0 FAIL, build i formatowanie PASS (szczegóły CHAT-R78). Wdrożenie stagingowe nie wykonano.

- C28: Backend `446116a45b387db64f8affcc56a679c0013622b2` wdrożony stagingowym skryptem; API healthy, readiness `Healthy`, obie migracje no-op (schemat jest aktualny). Format Delta sprawdzony testami Backend 7/7. Nie wysłano sztucznej wiadomości do cudzej rozmowy.

- C01: menu przypięcia reaguje callbackiem tylko na ACK. Cubit zwraca wynik typowany `succeeded/failed/ignored`; wyjątek nie pozostawia wiadomości w pending. Testy `chat_message_secondary_actions_test.dart` 8/8 PASS, `dart analyze` zakresu PASS. Runtime UI nie sprawdzono; brak widget testu zgodnie z decyzją użytkownika.
